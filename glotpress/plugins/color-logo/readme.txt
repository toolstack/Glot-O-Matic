GlotPress's logo in the top left of the screen is currently plain white text, but the GlotPress logo is actaully blue and white, this plugin makes the logo a little more colorful.

To enable this plugin, add the following lines to your gp-config.php file:

	define( 'GP_COLOR_LOGO', true );
